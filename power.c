
#include <stdio.h>
#include "power.h"

int power(int x, int i, int a)
{
    {
    for(x = 0; x < 3; x++) {
        a = x * i;
        printf("%d の %d乗 = %d¥n",power(x, i, a));
        return 0;
        }
    }
}
